# Debug 手记：从零实现 Deformable DETR 的过程中，我踩过的 5 个坑

> 本文记录这个项目真实发生过的 5 个 Bug：1 个「显存/性能」级问题、
> 1 个死循环、2 个数值语义问题、1 个索引规划问题。
> 每个都给出了现象、定位过程、根因、修复与教训。
> 它们比任何「一次写对」都更能说明工程判断力。

---

## 坑 1：手写 matmul 的「外积展开」差点把训练跑死（OOM / 性能）

### 现象

项目要求「手写 Matrix Multiplication 逻辑」。第一版 `manual_bmm` 严格按
矩阵乘法定义式实现：

```python
def manual_bmm(a, b):
    # C[b,i,j] = sum_k a[b,i,k] * b[b,k,j]
    return (a.unsqueeze(-1) * b.unsqueeze(-3)).sum(dim=-2)
```

教师模型在 128×128 输入下，编码器有 3 个尺度共 1344 个 token。
仅 query 投影一次就要显式展开 `B×I×K×J = 8×1344×128×128` 的外积
（约 880MB 的中间张量），一个编码器层里有 query/offset/attn/value/out
共 6~8 次投影，2 层编码器 + 反向传播后，峰值内存轻松超过 4GB；
即便没爆显存，CPU 上单步训练也要 30 秒——基本不可用。

### 定位

先用 `faulthandler` + 逐步计时拆出热点：矩阵投影部分占了大头；再分别计时
`manual_bmm` 的 `outer` 模式与 `torch.einsum`，前者慢 1~2 个数量级。

### 根因

`(B,I,K,J)` 的外积是把「定义式」**字面翻译**成内存操作：把每个中间乘积都
物化出来再求和。数学上正确，工程上是教科书写法——复杂度相同，但内存带宽
开销和峰值占用是融合内核的几十倍。

### 修复

1. `manual_bmm` 保留 `outer` 模式（教学/对拍用）；
2. 新增 `einsum` 模式：`torch.einsum("bik,bkj->bij", a, b)`，同一个求和公式，
   由底层优化内核收缩，训练速度提升 10~30 倍；
3. `manual_linear` 增加 `chunk_size` 分块，进一步限制峰值内存；
4. 用单测锁定两种模式与 `torch.matmul` 数学等价（见 `tests/test_ops.py`）。

### 教训

- **「从零实现」不等于「禁止用任何原语」**：算子的数学逻辑、采样、注意力
  权重全部自己写；收缩这步用 einsum 表达同一公式，是把工程效率与教学清晰度
  分开处理的成熟做法。面试时能把这个权衡讲清楚，比硬扛外积写法加分得多。
- 性能问题先用 profile 定位，不要凭感觉优化。

---

## 坑 2：自研匈牙利算法死循环（非方阵）

### 现象

匈牙利匹配器在 `(1, 4)` 这类「任务多于工人」的代价矩阵上**死循环**，
`make test` 整个卡住（pytest 超时）。

### 定位

用 `faulthandler.dump_traceback_later()` 抓到执行点：卡在 KM 算法内部；
加打印后发现某次迭代 `j1 = -1`，随后 `p[j0]` 用负数索引访问了数组末尾，
势能 `u/v` 被 `inf/NaN` 污染，算法从此无法收敛。

### 根因

移植 emaxx 的 O(n³) Kuhn-Munkres 实现时，我「顺手」把 1-based 下标改成
0-based：原算法的下标 0 是**虚拟哨兵列**，改成 0-based 后哨兵列和真实第 0 列
共用了同一个下标，匹配关系被破坏。这不是算法的 bug，是移植时丢了语义。

### 修复

忠实保留 1-based 数组（`u/v/p/way` 长度为 `n+1`，下标 0 为哨兵），
提取结果时再映射回 0-based；用 `scipy.optimize.linear_sum_assignment`
对拍覆盖方阵/非方阵/单行/单列等边界（见 `tests/test_ops.py`）。

### 教训

- 算法移植别「顺便改索引」，语义先于风格；
- 死循环类 bug：先 `faulthandler` 抓栈，再在关键循环加步数上限复现；
- 自研算法一定要有**参考实现对拍**，否则测试「碰巧通过」会掩盖问题。

---

## 坑 3：手写双线性采样与 `F.grid_sample` 的边界语义不一致

### 现象

对拍单测里，手写采样在**越界点**上与 `F.grid_sample(..., padding_mode="zeros")`
不一致（最大误差 ~1.7），而边界内的点完全一致。

### 定位

用「坐标编码特征图」`f(y,x) = y*100 + x` 逐个探针：插值结果直接暴露了
实际使用的源坐标。发现这个 torch 版本在 padding_mode="zeros" 下是
**逐角点丢弃**：某个角点的未裁剪坐标越界时，该角点整体不参与插值
（权重不再参与归一化）；而我第一版实现是「整体补零」。

### 根因

对边界语义的记忆（“越界输出 0”）在细节上错了：zeros padding 不是
「整点置零」，而是「越界角点丢弃」。数值库的边界细节只能以实测为准。

### 修复

按逐角点丢弃重写 `grid_sample_manual`，并补了覆盖 `[-1.2, 1.2]`
（含大量越界点）的对拍单测。

### 教训

- 数值语义别凭记忆，用可解码的探针数据实测；
- 对拍测试一定要覆盖**边界与越界**，只测有效区间会漏掉这类 bug。

---

## 坑 4：PyTorch 高级索引的维度顺序与直觉相反

### 现象

MSDeformAttn 中从 `(N, lq, nH, nP, nH, dv)` 取 head 对角线：

```python
sampled[:, :, head_idx, :, head_idx, :]   # 期望 (N, lq, nH, nP, dv)
```

小尺寸测试（`nH=2, n=2`）**碰巧通过**；换到真实尺寸
（`n=8, nH=4, lq=1344`）后形状变成 `(nH, n, lq, nP, dv)`，
维度顺序完全错位，注意力加权直接报错。

### 定位

打印每个步骤的形状，发现高级索引把广播出来的索引维放到了**最前面**，
而不是替换被索引维的位置；小尺寸时 `n == nH == 2`，两种解释的形状恰好相同，
掩盖了 bug。

### 修复

显式 `.permute(1, 2, 0, 3, 4)` 还原到 `(N, lq, nH, nP, dv)`；
同时把单测覆盖到「索引维长度与 batch 不同」的真实尺寸。

### 教训

- 高级索引（advanced indexing）的维度规则和普通切片不同，**不要凭直觉**；
- 「碰巧通过」的测试比没有测试更危险：单测参数要覆盖真实形状
  （各维长度互不相同）。

---

## 坑 5：MobileNetV3-Small 的「特征层索引」算错，蒸馏特征对不齐

### 现象

蒸馏特征对齐时报错：学生 stride-16 特征 `(2, 32, 2, 2)` vs 教师
`(2, 32, 4, 4)`，空间尺寸对不上。

### 定位

逐个打印各 bneck 输出的累计 stride，发现我把
`stem(2) -> b0(4) -> b1(8) -> b3(16) -> b8(32)` 的累计下采样节奏记错了，
导致取特征层的索引指向了 stride 32 的最后一层。

### 修复

画出主干的数据流表（每个 bneck 的 kernel/stride/输出通道/累计 stride），
按表取 stride 8（b1）与 stride 16（b7）的特征；蒸馏侧也改为
「按空间尺寸自动寻找与学生对齐的教师特征层」，从机制上杜绝这类错位。

### 教训

- 写带下采样的网络前，先画一张「模块 → 累计 stride → 输出通道」表；
- 跨网络特征对齐不要写死索引，用形状断言/自动匹配更稳。

---

## 调试方法论总结

1. **先复现、再二分**：把大问题拆成「算子级」小问题，逐层缩小范围；
2. **对拍优先**：自研实现必须和参考实现（torch/scipy）在覆盖边界的随机输入上
   对拍，锁定数学等价；
3. **形状打印 + 断言**：每个 reshape/permute/高级索引后打印形状，维度错误
   立刻暴露；
4. **性能热点用 profile**：不要凭感觉优化；先计时拆出热点再动手；
5. **测试参数要有区分度**：各维长度互不相同，避免「碰巧通过」。

> 如果你只有 3 天：不要新开项目，把上面任意一个坑展开成 3000 字的
> 「现象 → 复现 → 定位 → 修复 → 反思」，放进 GitHub Wiki。
> 面试官想看的不是「我会什么」，而是「遇到问题怎么思考」。
