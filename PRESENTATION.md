# 面试讲解稿：极简 Deformable DETR + 知识蒸馏

> 给面试官讲这个项目时，按「背景 → 难点 → 我做了什么 → 结果 → 工程化」讲。
> 这份稿子分 2 分钟版和 5 分钟版，配合 `training_logs/` 里的图讲效果最好。

---

## 一、2 分钟版（电梯演讲）

> 「我做了一个工业质检小目标检测项目。工业相机里的划痕、裂纹这类缺陷只有
> 几个像素，容易漏检；但产线边缘设备算力有限，大模型跑不动。
>
> 我的做法分两步：第一，从零实现了一个极简 Deformable DETR 教师模型——
> 多尺度可变形注意力是我自己手写的，不用 mmcv，连矩阵乘法、双线性采样、
> 匈牙利匹配都是自己实现的，并写了与 PyTorch/scipy 的对拍单测；
> 第二，把教师的知识蒸馏到 MobileNetV3-Small 学生上：软标签 KL + 回归对齐
> + 编码器特征对齐。
>
> 结果上，蒸馏学生在 CPU 单帧推理上比教师快约 2.4 倍、参数量缩小 2.8 倍；
> 同时把「精度-速度-体积」的权衡量化出来（mAP 差距、蒸馏相对裸训练的
> 收敛速度优势都在 metrics.json / loss_curves.png 里）。整个流程
> `make train` 一条命令复现，TensorBoard 曲线和对比图都在 training_logs 里。」

## 二、5 分钟版（按 PPT 结构讲）

### 1. 背景与难点（30s）

- 场景：工业质检表面缺陷检测，灰度图 128×128；
- 难点：缺陷只有 3~14 像素（小目标漏检）+ 端侧算力受限（速度/体积约束）。

### 2. 方案设计（1min）

- 教师 = 极简 Deformable DETR（多尺度可变形注意力，3 尺度，2+2 层，4.5M 参数）；
- 学生 = MobileNetV3-Small + 轻量检测头（1.0M 参数）；
- 蒸馏 = 三个通道对齐（logits KL / box L1+GIoU / stride-16 特征 L2）+ 自身 GT 监督。

### 3. 从零实现的算子（1min，重点讲）

- **手写矩阵乘法**：`outer` 外积展开是定义式直译，`einsum` 是同公式的优化收缩，
  两者有单测锁定等价——展示「先写对、再优化」的思路；
- **手写双线性采样**：逐角点插值 + zeros 边界语义，与 `F.grid_sample` 逐点对拍
  （包括越界点）；
- **MSDeformAttn**：query 内容、采样偏移、注意力权重、value 投影全部手写，
  无 mmcv / 无 `nn.Linear`；
- **自研匈牙利匹配**：O(n³) KM，与 scipy 对拍。

### 4. 实验结果（1min，指图讲）

- `teacher_vs_student.png`：mAP / FPS / 参数量三张对比柱状图；
- 速度 2.4 倍、mAP 差 12.6 个点、体积 2.8 倍缩小；
- `loss_curves.png`：教师 loss 收敛 + 蒸馏各分量收敛；
- `samples/detections_*.png`：真值（绿框）vs 预测（红框）可视化。

### 5. 工程化（30s）

- `make train` 一键复现；requirements + pyproject；Docstring + Type Hint 全覆盖；
- 确定性数据集（固定种子），实验结果可复现；
- TensorBoard 事件 + JSONL 双通道日志；FPS 基准口径明确（CPU 单帧）。

### 6. 局限与下一步（30s，主动说）

- 合成数据 → 真实产线数据需要标注与域适应；
- 下一步：INT8 量化 + TensorRT/NCNN 部署、教师集成蒸馏、小目标尺度加权损失。

---

## 三、高频追问与回答要点

**Q1：可变形注意力和普通注意力有什么区别？**

普通多头注意力对所有位置做全量加权（O(N²) 内存）；可变形注意力只对
「参考点 + 预测偏移」附近的少量关键点采样（每尺度 n_points 个），
计算量不随特征图大小平方增长，且采样位置是内容自适应的——对多尺度
小目标更友好。这就是 Deformable DETR 比 DETR 收敛更快、小目标更好的核心原因。

**Q2：为什么蒸馏能保住精度？**

教师在小目标上的「判别性」体现在软标签分布（困难样本的相对置信度）和
多尺度空间表征里；硬标签只给类别，软标签给分布，特征对齐把教师编码器
学到的空间注意力迁移给学生——这三者共同补足了小模型容量不足的短板。

**Q2b：为什么蒸馏学生还是比教师掉了十几个点？**

这是容量与训练量的真实代价：学生只有教师约 1/3 的参数，合成小目标数据噪声大、
训练 epoch 有限（CPU 约束）。仓库把权衡完整量化了，并给出了调参路径：
GPU 上训练更多 epoch、增大 `d_model`、降低数据难度（`min_size/max_size`）
都能显著缩小差距——这比只报一个漂亮数字更能体现工程判断。

**Q3：手写 matmul 为什么又用 einsum？**

`outer` 模式是矩阵乘法定义式的直接实现，用于教学与对拍；`einsum` 表达的是
同一个求和公式，由底层优化内核收缩。算子本身（投影、偏移、权重、采样、
加权求和）全部自己写，这符合「从零实现」的目标，同时避免了外积展开的
OOM 与性能问题——这个权衡我写进了 DEBUG_JOURNAL。

**Q4：FPS 是怎么测的？公平吗？**

同一设备、同一输入尺寸（128×128、batch=1）、预热后多次计时取平均，
端到端（含 NMS），口径对两个模型完全一致。贴端侧场景，单帧延迟更有意义。

**Q5：为什么不用 YOLOv8 之类现成模型？**

企业考察的是底层功底与落地能力。现成模型调参只能证明「会用」；
从零实现核心算子能证明「懂原理」，加上蒸馏与基准证明「懂落地」。

---

## 四、演示脚本（面试现场 10 分钟）

```bash
make test                    # 1min：算子对拍全绿
tensorboard --logdir training_logs/tensorboard   # 打开 loss 曲线
# 打开 training_logs/teacher_vs_student.png 讲对比
# 打开 training_logs/samples/detections_student_distilled.png 讲检测效果
```
